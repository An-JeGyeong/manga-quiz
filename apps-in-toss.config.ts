import { defineConfig } from '@apps-in-toss/web-framework/config'

export default defineConfig({
  appName: 'manga-quiz',

  brand: {
    primaryColor: '#D85A30'
  },

  permissions: [],
  webBundleDir: 'dist'
})
